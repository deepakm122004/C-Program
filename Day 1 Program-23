PLACING THE FLAG POST
#include <stdio.h>

int main() {
    int x, y, L;
    scanf("%d", &x);
    scanf("%d", &y);
    scanf("%d", &L);
    int center_x = x + L / 2;
    int center_y = y + L / 2;
    printf("x-coordinate:%d\n", x);
    printf("y-coordinate:%d\n", y);
    printf("Length:%d\n", L);
    printf("Centre of the ground is at (%d,%d)\n", center_x, center_y);
    
    return 0;
}
